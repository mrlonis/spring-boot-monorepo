# data

This folder groups the data-centric Spring Boot applications in the monorepo.

## Modules

- [mysql-migrations](mysql-migrations) - MySQL schema-evolution sample focused on `TIMESTAMP` to `DATETIME` migration strategies

For shared local ports and supporting-service ports, see the [root port catalog](../README.md#local-profiles-and-ports).
